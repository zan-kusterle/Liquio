<h1 class="title" style="margin-top: 0px;"><a href="/polls/<%= @poll.id %>""><%= @poll.title || @poll.id %></a></h1>

<div class="section-title">Decide whether the claim is <%= @pole %> reference.</div>
<p class="claim"><%= @reference.reference_poll.title %></p>

<%= render Democracy.HtmlPollView, "cast_vote.html", own_vote: @own_vote %>

<%= render Democracy.HtmlPollView, "score.html", poll: @reference.approval_poll %>

<a href="/polls/<%= @reference.approval_poll.id %>/details" class="link">Details</a>